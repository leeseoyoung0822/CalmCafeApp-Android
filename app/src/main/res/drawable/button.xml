<?xml version="1.0" encoding="utf-8"?>
<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 버튼이 클릭된 상태일 때의 배경 -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/beige" /> <!-- 클릭 시 배경 베이지색 -->
            <corners
                android:bottomLeftRadius="30dp"
                android:bottomRightRadius="30dp"
                android:topLeftRadius="30dp"
                android:topRightRadius="30dp" />
            <stroke
                android:width="1dp"
                android:color="@color/beige"/> <!-- 클릭 시에도 테두리 유지 -->
        </shape>
    </item>

    <!-- 버튼이 기본 상태일 때의 배경 -->
    <item android:state_enabled="true">
        <shape>
            <solid android:color="@color/white" /> <!-- 기본 배경 흰색 -->
            <corners
                android:bottomLeftRadius="30dp"
                android:bottomRightRadius="30dp"
                android:topLeftRadius="30dp"
                android:topRightRadius="30dp" />
            <stroke
                android:width="1dp"
                android:color="@color/beige"/> <!-- 기본 상태 테두리 베이지 -->
        </shape>
    </item>
</selector>